# Release signing values stay on the build machine. Never commit the keystore or passwords.
## Copy only this password entry to .env.signing.local, then run npm run signing:setup.
## The setup command supplies the external keystore path/alias defaults and writes the rest.
MULTIAGENT_ANDROID_KEYSTORE_PASSWORD=replace-with-at-least-20-characters
